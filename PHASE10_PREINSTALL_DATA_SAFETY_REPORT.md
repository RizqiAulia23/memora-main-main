# PHASE 10 — PRE-INSTALL DATA SAFETY REPORT

Date: 2026-08-17
Machine: Windows, drive C: (Micron NVMe 475.9GB) + D: USB (FEDORA-WS-L, not used for backup by user decision)
Project: `C:\Users\Riz\Documents\memora-main-main` (Laravel 13, PHP 8.3.33, SQLite)

## 1. Inventory (10A)

| Item | State |
|---|---|
| PHP | 8.3.33 (ZTS VS16 x64), Composer 2.4.1, Node v24.18.1, npm 11.16.0 |
| Framework | `laravel/framework` ^13.8 |
| Git | branch `main`, remote `https://github.com/RizqiAulia23/memora-main-main.git` |
| Latest commit | `8ced94d` "Harden file cleanup and add failure regression tests" |
| Uncommitted work | 217 entries (61 modified + 156 untracked) — all Phase 7/8/9 work |
| Database | `database/database.sqlite`, 430,080 bytes |

Database read-only inspection (PDO `mode=ro`): `PRAGMA integrity_check` = `ok`,
`journal_mode` = `delete`, schema_version 73, 24 tables with data:
users 10, memories 10, love_letters 10, favorites 6, connections 4,
important_dates 3, playlist_tracks 3, shared_playlists 2, shared_events 2,
bucket_list_items 2, notifications 3, sessions 87, cache 120, migrations 24,
user_settings 2, shared_memories 1; empty: cache_locks, failed_jobs, jobs,
job_batches, password_reset_tokens.

User uploads: 21 images in `storage/app/private/` (3 avatars + 18 memory
photos, incl. one 1.11MB JPG). `storage/app/public` contains only `.gitignore`;
no `public/storage` symlink exists (uploads live on the private disk).

`.env`: present with all standard Laravel variables (DB_CONNECTION=sqlite, no
DB_DATABASE override). Secret VALUES were never printed in reports.

## 2. Classification (10B)

- **Category A (irreplaceable):** `database/database.sqlite`, `storage/app/private/` uploads
- **Category B (secrets):** `.env`
- **Category C (source):** all project files incl. untracked Phase 7/8/9 work, `.git` history
- **Category D (regenerable):** `vendor/`, `node_modules/`, `public/build/`, storage framework/logs caches — intentionally NOT backed up (regenerated by `composer install`, `npm install && npm run build`)

## 3. Git safety audit (10C, 10H)

- Local `main` == `origin/main` at `8ced94d` — committed history is fully on GitHub.
- 217 uncommitted changes (incl. ALL Phase 9 animation modules: `motion-lifecycle.js`,
  `animation-tokens.js`, `memorify-animations.js`, 30 `*-animations.js` files, new
  controllers/models/policies/migrations/tests/views, reports) exist ONLY locally.
- DB, uploads, `.env` are gitignored — never on GitHub.
- No history rewrite, no force-push, no destructive git commands were executed.

## 4. Backup created (10D–10I)

Location: `C:\Users\Riz\BACKUP_MEMORIFY_2026-08-17\` (650 files)

| Content | Files | Verification |
|---|---|---|
| `database/database.sqlite` | 1 | SHA-256 match ✓ (`7a2fbf9b...50b`, 430080 bytes) |
| `config/.env` + `.env.example` | 2 | SHA-256 match ✓ |
| `storage/private/` uploads | 22 | 21/21 images hash-verified ✓ |
| `source/` (full project incl. `.git`) | 582 | 345/345 non-regenerable files present, 0 size mismatches ✓ |
| `reports/` + `reports/qa-scripts/` | 44 | phase reports + QA scripts (phase6–9, probes, migrate/fix scripts) |
| `restore/` | 1 | RESTORE_MEMORIFY_AFTER_WINDOWS_REINSTALL.md |
| `BACKUP_MANIFEST_SHA256.txt` | 1 | 650 entries, `SHA-256 size relative-path` per file |

## 5. Backup location safety (10K)

- Backup is on drive **C:** — the SAME physical disk (Micron NVMe) as Windows.
- User explicitly declined copying to the secondary D: USB drive.
- **Therefore: the backup will NOT survive a reinstall/format of C:.
  The project is NOT yet disaster-safe.**

## 6. Restore procedure (10J)

`BACKUP_MEMORIFY_2026-08-17\restore\RESTORE_MEMORIFY_AFTER_WINDOWS_REINSTALL.md`
covers: PHP/Composer/Node prerequisites, source restore, `.env` restore (do NOT
`key:generate`), database restore (do NOT run `migrate` on restored DB), uploads
restore, `composer install` / `npm install && npm run build`, read-only
verification steps, and the explicit list of commands that must never be run
pre-reinstall (`migrate:fresh`, `db:wipe`, `key:generate`, `git reset --hard`,
`git clean -fd`, diskpart/format).

## 7. Destructive operations performed

None. This phase was read-only + copy-only.
